The audit is thorough and accurate. All 12 findings confirmed against actual code — the 3 criticals are real capital
  risks.

  Key validation highlights:
  - C1: Router 15s vs REST 20s×3 retries = 61.5s worst case. Confirmed at order_router_service.py:155 vs
  bybit_client.py:976,988
  - C2: Comment says 60s but code does > 30.0 with zero exchange recheck. Confirmed at grid_bot_service.py:4644
  - C3: Both margin_monitor_service.check_all_positions() and _auto_margin_guard() run every cycle with no coordination.
   Confirmed at runner.py:968 and grid_bot_service.py:10799
  - H1: ws_timeout falls through implicit retCode=-1 path to REST — duplicate order risk after WS already sent.
  Confirmed at bybit_client.py:342,361
  - H5: _build_stop_cleanup_execution_payload() literally returns execution_blocked: False, viability_status: "viable"
  for cleanup-pending bots. Confirmed at entry_readiness_service.py:314-316

  The plan is 5 phases ordered by capital risk, each independently deployable with rollback triggers.

????????????????????????????????????????????????????????????????????????????????????????????????????????????????????????
 Ready to code?

 Here is Claude's plan:
????????????????????????????????????????????????????????????????????????????????????????????????????????????????????????
 Opus Trader Audit Remediation Plan

 Context

 A full-system audit identified 12 confirmed findings across execution, reconciliation, risk, and UI layers. Every
 critical and high finding was validated against actual code. The system runs live on mainnet with real capital — fixes
  are ordered by capital risk and grouped by shared code paths. Each phase is independently deployable.

 Validation Summary

 ?????????????????????????????????????????????????????????????????????????????????????????????????????
 ? ID  ?                               Finding                                ? Severity ?  Status   ?
 ?????????????????????????????????????????????????????????????????????????????????????????????????????
 ? C1  ? Router timeout (15s) < REST timeout (20s×3) — manufactured ambiguity ? CRITICAL ? CONFIRMED ?
 ?????????????????????????????????????????????????????????????????????????????????????????????????????
 ? C2  ? Divergence guard auto-clears after 30s with no exchange recheck      ? CRITICAL ? CONFIRMED ?
 ?????????????????????????????????????????????????????????????????????????????????????????????????????
 ? C3  ? Dual margin controllers — no shared ownership/budget                 ? CRITICAL ? CONFIRMED ?
 ?????????????????????????????????????????????????????????????????????????????????????????????????????
 ? H1  ? WS timeout after send falls through to REST — duplicate orders       ? HIGH     ? CONFIRMED ?
 ?????????????????????????????????????????????????????????????????????????????????????????????????????
 ? H2  ? hard_stop/auto_stop mark "stopped" without verifying flat            ? HIGH     ? CONFIRMED ?
 ?????????????????????????????????????????????????????????????????????????????????????????????????????
 ? H3  ? Risk state read-modify-write outside single lock span (TOCTOU)       ? HIGH     ? CONFIRMED ?
 ?????????????????????????????????????????????????????????????????????????????????????????????????????
 ? H4  ? Stop-loss max loss uses grid midpoint, ignores leverage/exposure     ? HIGH     ? CONFIRMED ?
 ?????????????????????????????????????????????????????????????????????????????????????????????????????
 ? H5  ? stop_cleanup_pending returns execution_blocked=False, viable         ? HIGH     ? CONFIRMED ?
 ?????????????????????????????????????????????????????????????????????????????????????????????????????
 ? H6  ? Runtime payload omits control booleans after config save             ? HIGH     ? CONFIRMED ?
 ?????????????????????????????????????????????????????????????????????????????????????????????????????
 ? H7  ? Cross-mode preview bypasses bounded preview constraint               ? HIGH     ? CONFIRMED ?
 ?????????????????????????????????????????????????????????????????????????????????????????????????????
 ? M1  ? Active Bots chips double-count armed/late in watch                   ? MEDIUM   ? CONFIRMED ?
 ?????????????????????????????????????????????????????????????????????????????????????????????????????
 ? M2  ? Bridge freshness not section-specific, open_orders ignored           ? MEDIUM   ? CONFIRMED ?
 ?????????????????????????????????????????????????????????????????????????????????????????????????????

 ---
 Phase 1: Order Execution Pipeline Safety (C1 + H1)

 Risk: Duplicate live orders, exchange state divergence from local truth.

 C1 — Router timeout alignment

 File: services/order_router_service.py
 - Line 155: change default_timeout_sec: float = 15.0 ? 25.0 (20s REST + 5s grace)
 - Add runtime warning if default_timeout_sec < _EXCHANGE_CALLBACK_TIMEOUT_SEC

 H1 — WS timeout = ambiguous, never resend

 File: services/bybit_client.py
 - In _try_ws_create_order() (line 329), add explicit ws_timeout check BEFORE the retCode fallback at line 342
 - Return ambiguous result dict with retry_safe=False, truth_check_required=True instead of None (which triggers REST
 fallback)

 Verify

 - All 1024+ tests pass
 - Restart services, grep logs for "ws_timeout_after_send" — should never be followed by REST fallback

 ---
 Phase 2: Cleanup & Divergence Truth (C2 + H2 + H5)

 Risk: New entries resume with orphaned orders; false-flat state after partial fills.

 C2 — Divergence guard requires exchange recheck

 File: services/grid_bot_service.py
 - Line 4644: replace time-based auto-clear with _confirm_symbol_cleanup_state() call
 - 30s threshold becomes "recheck trigger" not "auto-clear trigger"
 - If exchange still diverged, update timestamp and keep blocker active

 H2 — All stop paths through cleanup state machine

 File: services/grid_bot_service.py
 - _complete_session_timer() (line 1670): after close attempt, call _confirm_symbol_cleanup_state() — enter
 stop_cleanup_pending if not flat instead of immediately setting stopped
 - auto_stop (line 12396): same pattern — verify flat before finalizing stopped
 - Existing _monitor_stop_cleanup_pending loop (line 3710) already handles resolution

 H5 — stop_cleanup_pending = execution blocked

 File: services/entry_readiness_service.py
 - Line 314: execution_blocked: False ? True
 - Line 315: execution_viability_status: "viable" ? "blocked"
 - Line 316: execution_viability_reason: "openings_clear" ? "stop_cleanup_pending"

 Verify

 - All tests pass
 - Stop a bot with open position ? must enter stop_cleanup_pending before stopped
 - UI shows blocked (not viable) during cleanup

 ---
 Phase 3: Margin Controller Arbitration (C3)

 Risk: Both controllers add capital to same stressed position simultaneously.

 Design: Per-bot auto_margin owns active symbols, global monitor is safety net for unowned positions

 File: services/margin_monitor_service.py
 - In check_all_positions() (line 231): build set of symbols with active bot auto_margin enabled
 - Skip those symbols in the per-position loop — per-bot _auto_margin_guard handles them
 - Log deferred symbols for visibility

 File: services/auto_margin_mixin.py (or grid_bot_service.py)
 - Add per-cycle budget cap to _auto_margin_guard (2× single-add cap)
 - Clear cycle budget at start of each bot cycle

 Verify

 - All tests pass
 - Monitor MarginMonitor deferring log — confirms symbol delegation
 - Running bot with auto_margin enabled gets margin from one source only

 ---
 Phase 4: Independent High Fixes (H3, H4, H6, H7)

 Four independent fixes in separate files — can be implemented in parallel.

 H3 — Atomic risk state updates

 File: services/risk_manager_service.py
 - Add _read_modify_write(modifier_fn) method — read + modify + write under single exclusive flock
 - Refactor update_equity_state() (line 161) and record_symbol_daily_trade() to use it
 - Eliminates TOCTOU race on kill-switch state, peak equity, daily loss

 H4 — Stop-loss max loss from actual entry + leverage

 File: services/stop_loss_service.py
 - Line 244: use actual_entry_price (from position avgPrice) instead of grid midpoint when available
 - Lines 247-257: divide max_loss_pct by leverage to get effective max price move
 - effective_max_move = max_loss_pct / max(bot_leverage, 1.0)

 H6 — Runtime payload carries control booleans

 File: services/bot_status_service.py
 - In runtime payload builder: include trailing_sl_enabled, quick_profit_enabled, recovery_enabled,
 btc_correlation_filter_enabled from persisted bot state
 File: static/js/app_lf.js
 - In reportPostSaveConfigRuntimeTruth() (line 13834): when mismatches detected, trigger delayed re-fetch

 H7 — Bounded preview enforced on mode matrix

 File: services/bot_status_service.py
 - In _build_mode_readiness_matrix() (line 3252): check if stopped bot is outside bounded preview window
 - If outside window, return preview_disabled for all candidate modes instead of running full evaluation

 Verify

 - All tests pass
 - H3: concurrent risk updates produce consistent state
 - H4: SL placement accounts for leverage (e.g., 10x leverage ? 1/10th the price distance)
 - H6: save config ? UI reflects new values within 2s
 - H7: stopped bot outside preview window ? mode matrix shows preview_disabled

 ---
 Phase 5: UX Corrections (M1, M2)

 No capital risk — display correctness only.

 M1 — Dedup Active Bots chips

 File: static/js/app_lf.js
 - Lines 9090-9104: subtract armed/late count from watch count so totals are mutually exclusive

 M2 — Bridge section-specific freshness

 File: app.py
 - In _prefer_runtime_snapshot_bridge() (line 1245): require positions and open_orders sections to be non-stale
 - Consume bridge open_orders section in dashboard data assembly

 Verify

 - Armed bots don't inflate watch count
 - Stale open_orders section prevents bridge preference

 ---
 Phase Dependencies

 Phase 1 (C1+H1) ??? Phase 2 (C2+H2+H5) ??? Phase 3 (C3) ??? Phase 4 (H3+H4+H6+H7) ??? Phase 5 (M1+M2)

 Each phase: run full test suite ? restart both services ? verify PIDs ? create project zip.

 Rollback Triggers

 ????????????????????????????????????????????????????????????????????????????????????????????????
 ? Phase ?                  Trigger                  ?                  Action                  ?
 ????????????????????????????????????????????????????????????????????????????????????????????????
 ? 1     ? Duplicate orders or order failures        ? Revert timeout values                    ?
 ????????????????????????????????????????????????????????????????????????????????????????????????
 ? 2     ? Bots stuck in stop_cleanup_pending > 5min ? Add 300s failsafe with exchange recheck  ?
 ????????????????????????????????????????????????????????????????????????????????????????????????
 ? 3     ? Margin not added when needed              ? Remove symbol-exclusion in MarginMonitor ?
 ????????????????????????????????????????????????????????????????????????????????????????????????
 ? 4     ? Test failures or state corruption         ? Revert individual fix independently      ?
 ????????????????????????????????????????????????????????????????????????????????????????????????
 ? 5     ? Chip counts look wrong                    ? Revert JS changes                        ?
 ????????????????????????????????????????????????????????????????????????????????????????????????